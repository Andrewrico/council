---
name: orchestration-fanout
description: Ensemble cross-check and multi-agent routing: poll operators via the operators-mcp council tool, fan out subagents, synthesize agreement/dissent into one recommendation.
metadata:
  source-operator: claude
  selected-by: council-capability-matrix.md (Stage 2 SELECT, 2026-07-02)
---

# orchestration-fanout

**Inherited from:** `claude` — selected as the best verified implementation of this
dimension in the Stage 2 capability matrix.

## What Council does with it (advisory-only)

- Poll 3+ relevant operators via the `council` MCP tool before a contested recommendation.
- Name agreement level and dissent explicitly; never average away a minority warning.
- Route the actual work to the named specialist — Council never executes.

## Evidence (files actually read)

operators-mcp registry entry (capabilities: orchestration, planning, decomposition); 42 harvested skills incl. swarm-orchestration, stream-chain, subagent-tier-picker; ranking #1 (council spec §3).

> Council is DORMANT (PLAN_ONLY floor). This skill informs recommendations; it never
> authorizes execution. See ../../CONSTITUTION.md.
